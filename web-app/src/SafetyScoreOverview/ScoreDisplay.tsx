import React, { useEffect, useState } from 'react';
import '../App.css'

const ScoreBar: React.FC = () => {
  return (
    <div className="ScoreBar">
        Score display (with score bar) be like
    </div>
  );
}

export default ScoreBar;